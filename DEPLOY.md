# 🚀 Инструкция по деплою на GitHub Pages

## Шаг 1: Подготовка репозитория

1. Создайте новый репозиторий на GitHub (например, `calendar-tracker`)
2. Откройте терминал в папке с проектом

## Шаг 2: Настройка конфигурации

Откройте файл `package.json` и измените строку:
```json
"homepage": "https://ВАШ_USERNAME.github.io/calendar-tracker"
```

Например, если ваш username `ivanov123`, то:
```json
"homepage": "https://ivanov123.github.io/calendar-tracker"
```

Откройте файл `vite.config.ts` и измените:
```typescript
base: '/calendar-tracker/',
```

## Шаг 3: Установка зависимостей

```bash
npm install
```

## Шаг 4: Инициализация Git и загрузка кода

```bash
git init
git add .
git commit -m "🎉 Initial commit - Calendar Tracker"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/calendar-tracker.git
git push -u origin main
```

## Шаг 5: Деплой

```bash
npm run deploy
```

Эта команда автоматически:
- Соберет проект (`npm run build`)
- Создаст ветку `gh-pages`
- Загрузит собранные файлы в эту ветку

## Шаг 6: Настройка GitHub Pages

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе "Source" выберите ветку `gh-pages`
3. Папку оставьте `/ (root)`
4. Нажмите `Save`

Через 1-2 минуты ваше приложение будет доступно по адресу:
`https://ВАШ_USERNAME.github.io/calendar-tracker`

## Обновление приложения

Когда внесете изменения в код:

```bash
git add .
git commit -m "описание изменений"
git push
npm run deploy
```

## Проверка локально перед деплоем

```bash
npm run dev
```

Откройте http://localhost:5173

## Возможные проблемы

### Приложение не открывается
- Проверьте, что в Settings → Pages выбрана ветка `gh-pages`
- Подождите 2-3 минуты после деплоя

### Белая страница после деплоя
- Убедитесь, что в `package.json` правильно указан `homepage`
- Убедитесь, что в `vite.config.ts` указан правильный `base`

### Данные не сохраняются
- Это нормально! Данные хранятся в localStorage вашего браузера
- При очистке кэша браузера данные удалятся
- На разных устройствах будут разные данные

## 🎉 Готово!

Теперь у вас есть работающий мобильный календарь с долговременным хранением данных!
