# 📅 Календарь-трекер привычек

Мобильное приложение для отслеживания привычек и дней в календаре с долговременным хранением данных.

## 🚀 Возможности

### Основные функции
- ✅ **Отметка дней** - зачёркивайте дни одним касанием
- 📋 **Чек-листы** - для каждого дня доступны 4 привычки:
  - 🚫 Без алкоголя
  - 💧 Вода (2л)
  - 👟 Шаги (10k)
  - 🥗 Правильное питание
- 🏋️ **Дни тренировок** - автоматически отмечены понедельники, среды и пятницы
- 📊 **Индикаторы прогресса** - визуальное отображение выполненных привычек
- 💾 **Долговременное хранение** - данные сохраняются в localStorage (месяцы и дольше)

### Дизайн
- 📱 Адаптивный дизайн для мобильных устройств
- 🎨 Современный интерфейс с плавными анимациями
- 🎯 Интуитивное управление
- ⚡ Быстрая работа без интернета

## 🎯 Как пользоваться

### Просмотр календаря
- **Сегодняшний день** - обведён фиолетовой рамкой
- **Дни тренировок** - помечены иконкой гантели (🏋️)
- **Прогресс привычек** - показан точками под числом (до 4 точек)

### Работа с днями
1. **Нажмите на день** - откроется панель с чек-листом
2. **Отмечайте привычки** - нажимайте на чекбоксы
3. **Зачёркивание дня** - кнопка с крестиком в правом верхнем углу панели
4. **Закрыть панель** - нажмите на день ещё раз или на другой день

### Навигация
- **Стрелки ← →** - переключение между месяцами
- **Статистика** - показывает количество зачёркнутых дней в месяце

## 📦 Установка

```bash
npm install
```

## 🛠️ Разработка

```bash
npm run dev
```

Откройте [http://localhost:5173](http://localhost:5173) в браузере.

## 📤 Деплой на GitHub Pages

### Первоначальная настройка

1. Создайте репозиторий на GitHub
2. Инициализируйте git и загрузите код:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_REPO.git
git push -u origin main
```

3. В `package.json` измените поле `homepage`:

```json
"homepage": "https://ВАШ_USERNAME.github.io/ВАШ_REPO"
```

4. В `vite.config.ts` измените `base`:

```typescript
base: '/ВАШ_REPO/',
```

### Деплой

```bash
npm run deploy
```

После деплоя приложение будет доступно по адресу: `https://ВАШ_USERNAME.github.io/ВАШ_REPO`

### Настройка GitHub Pages

1. Перейдите в Settings → Pages вашего репозитория
2. В Source выберите ветку `gh-pages`
3. Сохраните настройки

## 💡 Технологии

- React 18
- TypeScript
- Vite
- Lucide React (иконки)
- localStorage (хранение данных)

## 📱 Структура данных

Для каждого дня хранится объект:
```typescript
{
  marked: boolean,      // День зачёркнут
  alcohol: boolean,     // Без алкоголя
  water: boolean,       // Выпита вода
  steps: boolean,       // Пройдены шаги
  nutrition: boolean    // Правильное питание
}
```

## 🔒 Приватность

Все данные хранятся локально в вашем браузере. Никакие данные не отправляются на сервер.

## 🎨 Кастомизация

### Изменить дни тренировок
В файле `src/CalendarApp.tsx` найдите функцию `isWorkoutDay`:
```typescript
const isWorkoutDay = (day: number) => {
  const dayOfWeek = date.getDay();
  // Измените дни: 1 = Пн, 2 = Вт, 3 = Ср, 4 = Чт, 5 = Пт, 6 = Сб, 0 = Вс
  return dayOfWeek === 1 || dayOfWeek === 3 || dayOfWeek === 5;
};
```

### Изменить привычки
В панели деталей дня измените текст и эмодзи для чекбоксов.

## 📄 Лицензия

MIT
